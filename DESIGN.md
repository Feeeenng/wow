# WoW Recorder 客户端设计规范

本文档约束 Windows 客户端后续页面与组件的视觉和交互实现。目标是保持界面扁平、清晰、可扫描，并允许按《魔兽世界》版本统一切换主题。

## 1. 技术边界

- 基础组件统一使用 Ant Design 6，禁止重复实现按钮、输入框、选择器、弹窗和反馈组件。
- 页面布局、间距、响应式和少量状态样式使用 Tailwind CSS。
- 图标优先使用 `@ant-design/icons`，同一操作不得混用不同图标体系。
- React 只负责界面和交互，系统路径、进程、下载、安装、OBS WebSocket 和本地配置均由 Tauri Rust 后端处理。
- `client/src` 内部导入统一使用 `@/` 别名，不使用 `../` 或 `./` 跨模块导入。

## 2. 视觉风格

- 使用扁平化界面，不使用渐变、发光球、拟物纹理或装饰性边框。
- 页面背景、面板背景和边框只保留清晰的三级层次；面板圆角不超过 `6px`。
- 不在卡片中嵌套卡片。设置分组使用带标题的平面区块，状态列表使用分隔线表达层级。
- 页面标题为 `20px`，分组标题为 `16px`，正文为 `14px`，辅助文字为 `12px`。
- 桌面客户端默认窗口为 `1440×900`，最小尺寸为 `1080×720`，启动时居中且不默认最大化；窄窗口下设置内容允许纵向排列，不允许文字和控件重叠。

## 3. 主题系统

- 全局主题唯一入口为 `client/src/app/theme.ts`。
- Ant Design Token 与 CSS 变量必须由同一主题预设生成，业务组件不得硬编码品牌色。
- 主题预设以魔兽世界版本命名，可调整主色、强调色、页面背景、面板背景、边框和状态色。
- 新版本主题只新增预设，不复制页面样式；切换主题后所有导航、按钮、链接、焦点态和状态色同步变化。

当前基础变量：

| 变量 | 用途 |
| --- | --- |
| `--app-primary` | 主按钮、选中态、链接 |
| `--app-accent` | 版本强调色和关键数据 |
| `--app-bg` | 应用页面背景 |
| `--app-surface` | 导航和内容面板背景 |
| `--app-border` | 分隔线和控件边框 |
| `--app-text` | 主要文字 |
| `--app-text-secondary` | 辅助文字 |

## 4. 布局与导航

- 主导航固定为：首页、回放、直播、个人中心、设置。
- 设置页使用二级导航承载 OBS、战斗日志及其他客户端配置，首页不直接展示配置表单。
- 左侧主导航宽度固定，顶部标题栏承载页面标题、用户入口和窗口控制。
- 设置表单标签、输入控件和操作按钮对齐；高频主操作使用实色按钮，次要操作使用默认按钮。

## 5. 组件与状态

- 页面必须覆盖加载、成功、空数据、错误和禁用状态。
- 异步操作必须显示按钮加载态，错误通过 Ant Design `Alert` 或 `message` 呈现。
- 开关使用 `Switch`，枚举使用 `Select` 或 `Segmented`，数值范围使用 `Slider`，路径使用只读输入框配合选择按钮。
- 危险操作使用 Ant Design 危险按钮并在执行前确认；普通停止录制属于明确命令，不使用危险色制造误导。
- 所有图标按钮必须提供 `title` 或 Tooltip。

## 6. CSS 编写规则

- CSS 每条声明单独占一行，禁止将多个声明并排书写。
- 主题值必须引用全局变量；仅布局尺寸、透明度和中性色可在局部样式中出现。
- 优先使用 Tailwind 工具类。仅当复杂状态、窗口拖拽区域或第三方组件覆盖无法清晰表达时编写自定义 CSS。

## 7. OBS 设置中心

- OBS 设置页以 `docs/images/setting-obs.png` 为结构参考：设置二级导航在内容区顶部横排；下方使用等宽双列，运行状态与下载安装在左，画面与声音设置在右；窄窗口下改为纵向排列。
- OBS 首次创建专属场景时，视频输出默认初始化为 `1920×1080`；该录制分辨率与客户端窗口尺寸无关。
- 设置项选择后立即同步到 OBS，不提供额外的“保存设置”按钮。
- 编码器、捕捉类型、目标窗口、声音设备、音量推子、实时电平、运行状态和输出目录必须读取 OBS 真实数据，不在 React 中维护伪造选项；未连接时编码器显示“内置”，声音设备以灰色“默认”占位，不生成演示数据或模拟 dB。
- 运行状态只展示连接状态、运行时长、录制条件、当前状态和输出位置，不展示 WebSocket 地址、协议版本、鉴权、便携模式或启动参数。
- 安装区域面向用户只使用“OBS Studio”“下载并安装”“检查并更新”等产品文案，不暴露压缩包、PDB、便携标记或运行时目录结构。
- 进入 OBS 设置页时立即检测安装状态。未安装时只显示自动下载安装入口，不显示安装目录；安装后下载区域切换为安装、运行和配置状态检测。
- OBS 安装期间必须显示当前阶段、实际下载百分比和进度条；校验、解压及配置阶段使用明确状态文案，安装按钮在任务结束前不可重复点击。
- OBS 未连接时，声音区域保留灰色的扬声器和麦克风两个通道，设备显示“默认”，不显示“必须连接”或“未检测”的说明；连接后由 OBS 数据点亮设备下拉和音量推子，电平条与 dB 只显示 OBS WebSocket 音量表事件的真实值。
- 扬声器和麦克风由 Rust/OBS 自动创建并启用，声音区域不显示启用复选框；用户只选择设备和调整音量。
- OBS 默认安装在客户端可执行文件同级 `obs` 目录；界面不提供自定义安装位置。
- 客户端运行期间由 Rust 持续守护 OBS。用户关闭 OBS 后自动重新启动并恢复专属场景连接。
- 客户端退出时先正常关闭由自身启动的 OBS；仅在等待超时后强制结束。便携 OBS 启动新进程前清理自身 `.sentinel` 异常退出标记；受管进程长时间无法提供 WebSocket 时结束并在下一轮自动重启，避免开发热重载或系统强制退出后被安全模式对话框阻塞。
- `WoW Recorder` 场景固定包含“魔兽世界游戏画面”来源，并按 1920×1080 画布等比填充；游戏捕捉、窗口捕捉、自动捕捉和目标窗口均以 OBS 实际提供的类型与属性列表为准，选择后由 Rust 写回 OBS。
- “开始测试录制”只有在 OBS 实时枚举到魔兽世界窗口且专属来源绑定正常时才可点击，Rust 开始录制命令必须执行相同校验。
- “开始直播”与“开始测试录制”在运行状态区并排展示，分别控制 OBS Virtual Camera 和录像输出；两者可以同时运行。未检测到魔兽世界窗口时均不可启动，运行后按钮分别切换为“停止直播”和“停止录制”。

## 8. 个人直播视角

- 直播页以 `docs/images/live-self.png` 为个人视角内容参考，上方为内容区全宽的 16:9 OBS 真实画面；直播视角、当前个人身份和团队视角禁用状态统一横排放在画面下方，窄窗口下再纵向排列。
- 当前只开放个人视角；团队视角可以显示禁用入口，但不得生成虚假成员、观看人数、副本进度或直播状态。
- 页面不展示麦克风、系统声音、推流质量、停止直播、网络质量、码率、延迟和丢包控件或指标。
- 画面通过 OBS Virtual Camera 播放：Rust 校验专属场景并控制虚拟摄像头输出，React 只申请和挂载系统视频轨；无画面时必须展示 OBS、场景、虚拟摄像头或游戏窗口的真实缺失原因。
- 视频区域使用稳定的 16:9 比例和 `object-cover` 填满内容区宽度，不得因页面剩余高度拉成接近方形或出现无意义黑边。
- 左上角在系统视频轨实际播放后只显示红色状态标志和“直播中”，不得追加来源、分辨率、观看人数或其他标签。
- 画面内控制只保留播放暂停和全屏，不得暗示已经向云端开始或停止推流。
- 无直播画面时只显示真实缺失原因和“去直播”主按钮，由该入口进入 OBS 直播设置；不显示“重新加载”或“检测 OBS 设置”。
