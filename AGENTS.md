# AGENTS.md

本文件定义自动化 Agent 在本仓库中的默认工作规则。目标是以最小、清晰、可验证的改动完成任务。

开始任何任务前，必须先读取根目录 `CLAUDE.md`，了解技术栈、目录架构、开发方式和测试约定；涉及产品行为时还必须读取 `docs/PRODUCT.md`。

# 项目编码规则
项目所有文档必须用简体中文表述
用于减少常见 LLM 编码错误的行为准则。可按需与项目特定指令合并使用。

权衡：这些准则更偏向谨慎而不是速度。对于很小的任务，可以结合实际判断。

1. 编码前先思考

不要假设。不要掩盖困惑。主动说明取舍。

实现前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解，列出来，不要静默选择其中一种。
- 如果有更简单的方案，要说出来。必要时提出反对意见。
- 如果某处不清楚，就停下来。说清楚困惑点，再提问。

2. 简单优先

用能解决问题的最少代码。不要写推测性代码。

- 不添加用户没有要求的功能。
- 不为只使用一次的代码创建抽象。
- 不添加用户没有要求的“灵活性”或“可配置性”。
- 不为不可能出现的场景添加错误处理。
- 如果你写了 200 行，但本可以用 50 行解决，就重写。

问自己：“资深工程师会不会认为这过度复杂？”如果答案是会，就简化。

3. 精准修改

只修改必须修改的地方。只清理你自己造成的问题。

编辑现有代码时：

- 不顺手“改进”相邻代码、注释或格式。
- 不重构没有坏掉的东西。
- 保持现有风格，即使你会用不同方式实现。
- 如果发现无关的死代码，可以提出来，但不要删除。

当你的改动产生孤立代码时：

- 移除由你的改动导致未使用的 import、变量和函数。
- 不删除原本就存在的死代码，除非用户明确要求。

检验标准：每一行修改都应该能直接追溯到用户请求。

4. 目标驱动执行

定义成功标准。循环推进，直到完成验证。

把任务转化为可验证的目标：

- “添加校验” -&gt; “为非法输入编写测试，然后让测试通过”
- “修复 bug” -&gt; “编写能复现 bug 的测试，然后让测试通过”
- “重构 X” -&gt; “确保重构前后测试都通过”

对于多步骤任务，先给出简短计划：

1. [步骤] -&gt; 验证：[检查项]
2. [步骤] -&gt; 验证：[检查项]
3. [步骤] -&gt; 验证：[检查项]

清晰的成功标准可以让你独立循环推进。模糊的标准（例如“让它能用”）需要持续澄清。

如果这些准则有效，表现应该是：diff 中不必要的改动更少，因过度复杂导致的返工更少，澄清问题会发生在实现前，而不是错误发生后。

## 1. 指令优先级

发生冲突时，按以下顺序执行：

1. 当前会话中用户的明确要求
2. 仓库内更具体目录中的 `AGENTS.md`
3. 根目录 `CLAUDE.md` 中的项目架构与技术约束
4. 其他仓库文档、现有代码和工具约定
5. 本文件

不确定需求、边界或兼容性影响时，先说明假设与取舍；高风险或不可逆操作必须先征得用户确认。

## 2. 工作原则

- 先理解再修改：阅读相关文档、入口、调用链和现有实现。
- 简单优先：只实现当前需求，不添加推测性功能、配置或抽象。
- 精准修改：不顺带重构、格式化或清理无关代码。
- 保持兼容：除非用户明确要求，不改变公开 API、数据库行为和用户可见行为。
- 目标驱动：多步骤任务先给出简短计划，并明确每一步的验证方式。
- 如实交付：不得虚构命令、退出码、测试结果或完成状态。
- 保护现场：保留用户已有改动；发现无关问题只说明，不擅自处理。

## 3. 项目约束

- 项目文档、Agent 回复和业务注释使用简体中文；代码标识符使用英文。
- 所有业务时间统一使用中国标准时间 `Asia/Shanghai`，不得将 CST 理解为美国中部时间。
- 数据库查询必须参数化；不得使用不可信输入拼接 SQL 或 Shell 命令。
- 配置、默认值、路径、开关、限制和 endpoint 统一放入项目配置模块，业务代码不得复制配置或引入魔法值。
- 新增配置项必须有简短中文注释，说明用途和影响范围。
- 未经用户明确要求，不修改依赖、根配置、CI、环境模板、数据库结构或迁移。
- 桌面客户端可恢复的用户设置统一由 Rust `LocalStateStore` 按业务区段写入 `client-state.json`；React 不得自行使用 `localStorage` 保存业务状态。
- 跨页面但仅在当前进程有效的状态应提升到应用级 Provider，页面组件不得因导航卸载而反复创建同一业务状态。
- 外部连接、进程、录制、安装进度、实时电平和运行时长属于瞬时真值，不得作为持久化状态直接恢复；启动后必须从 Rust 或外部系统重新检测。
- 不使用非 Git 工具修改 `.git`，不执行 `git reset --hard` 等破坏性命令。

## 4. 通用代码规范

- 遵循现有目录结构、命名、格式和框架惯例。
- 一个模块只承担一个清晰职责；按业务边界组织代码，不按文件类型机械拆分。
- 仅当稳定逻辑存在多个真实消费者时才抽取公共模块。
- 函数保持短小、低嵌套、单一职责；参数过多时优先使用上下文对象或领域对象。
- 文件超过 500 行或混合三类以上职责时，先评估拆分，禁止继续无边界堆叠。
- 边界条件必须显式处理；错误信息应包含可定位问题的必要上下文，但不得泄露敏感信息。
- 注释解释业务约束、设计原因和非显而易见的边界，不复述代码。
- 只清理由本次改动造成的未使用 import、变量、函数和文件。
- 重构应先保持行为不变；迁移模块时一次更新生产代码与测试导入，不保留薄包装或旧路径兼容别名。

## 5. 模块化与目录设计

- 新增代码前必须先明确所属业务域、模块职责、调用方和依赖方向；无法用一句话说明职责的模块不得开始实现。
- 目录按业务能力和共同变化关系组织，禁止仅按 `components`、`utils`、`handlers`、`types` 等文件类型机械堆放跨业务代码。
- 同一业务域出现两个及以上协作模块时，应建立领域目录；领域目录根部只保留入口、领域级契约和真正跨子模块共享的代码，具体实现继续按子能力分组。
- 禁止把安装、配置、协议适配、状态管理、业务编排和界面展示等不同职责平铺在同一目录。一个目录出现五个及以上业务实现文件，或同时承载三类及以上职责时，新增代码前必须先完成领域拆分评估。
- 禁止为单个短文件创建深层目录；只有一项职责且没有协作模块时保留单文件，出现真实协作关系后再升级为目录。
- 禁止为了“看起来模块化”创建只有转发、重导出或透传调用的包装层。调用方应直接依赖真实实现模块；`mod.rs`、`index.ts` 和包入口只声明模块或稳定公共 API，不承载业务逻辑，也不为旧路径提供兼容别名。
- 模块依赖必须单向：入口/界面/API -> 业务编排 -> 领域能力 -> 外部协议或基础设施。禁止领域模块反向依赖页面、命令注册或应用启动入口，禁止循环依赖。
- 数据契约、运行状态、配置解析、外部协议适配和业务操作按职责分离；只有同一领域内被多个真实模块消费的类型或工具才能进入领域共享模块。
- 页面入口、Tauri `lib.rs`、Rust `mod.rs`、Python API 路由和应用启动文件只负责注册、依赖注入与模块组合，不得持续堆叠业务实现。
- 新增或修改模块时同步检查同目录文件数量、单文件行数、跨域导入和重复实现；发现已达到拆分条件时，不得继续向现有大文件或平铺目录追加代码。
- 测试与被测业务模块就近放置或使用对应领域测试目录；迁移模块时必须一次更新生产代码、测试、命令注册和文档路径。
- Rust/Tauri 领域使用子目录表达稳定边界。例如 OBS 领域中，运行时安装、配置、连接和进程生命周期属于 `runtime/`，音频、画面捕捉和视频参数属于 `settings/`；后续新增能力必须进入对应领域，不得重新平铺到 `obs/` 根目录。
- 前端页面专属组件、Hook、服务和类型应放在对应页面或业务功能目录；只有业务中立且存在多个真实消费者的内容才能进入公共目录。
- Python 后端同一业务域的查询、计算、外部适配和序列化模块统一放在 `services/<domain>/`，API 层不得成为业务实现的临时堆放位置。

## 6. 前端规范

- 如仓库存在 `DESIGN.md`，实现页面或组件前必须先阅读并遵循。
- 前端 `src` 目录内的项目模块、组件、类型和样式统一使用 `@/` 路径别名导入；禁止使用 `./`、`../` 或 `../../` 相对导入，第三方依赖保持包名导入。
- CSS 规则必须使用多行格式：选择器与左花括号作为规则起始行，每条声明单独占一行，右花括号单独占一行；禁止将完整规则或多条声明并排压缩在同一行。
- 页面入口只负责权限、页面状态、数据请求和模块组合，不堆叠列定义、表单、弹层和格式化逻辑。
- 页面专属组件放在页面目录；只有业务中立且存在多个消费者的组件才进入公共组件目录。
- 表格、工具栏、详情、表单、Drawer、导出和 hooks 按共同变化关系拆分；不创建只有一个短文件的深层目录。
- 权限由页面或上层业务容器计算后通过明确 props 传入；通用组件不得默认管理员身份或扩大数据范围。
- 列、筛选项和操作菜单优先配置驱动，避免重复分支。
- 非显而易见的业务函数、自定义 Hook、权限判断、输入限制和状态转换应添加简短中文 JSDoc 或注释。
- 前端不得对后端已格式化的业务时间再次进行时区转换。
- 修改页面时同时检查加载、空数据、错误、禁用和无权限状态，并保证常见桌面与移动视口下内容不溢出或重叠。

## 7. 后端规范

- API 层只负责依赖注入、参数校验、调用 Service 和统一响应，不编写 SQL、聚合计算或跨资源流程。
- Service 使用 class 承载数据库会话、当前用户和运行时上下文；无状态纯工具逻辑可使用模块级函数。
- 一个 Service 只负责一个业务能力；查询、纯计算、外部协议适配和响应序列化按职责分离。
- 同一业务域出现多个协作模块时，组织到 `services/<domain>/`；调用方直接导入真实实现模块，不通过聚合入口转发。
- 涉及用户、actor 或租户范围的查询，必须先应用权限范围，再应用用户筛选条件。
- 后端新增或修改的 class、函数和方法必须包含简短中文 docstring，说明用途或关键约束。
- 数据库时间必须使用 `utils.time_utils` 中的统一工具，例如 `cst_now`、`normalize_to_cst` 和 `serialize_cst_datetime`。
- 不得直接使用 `datetime.utcnow()`、`datetime.now(UTC)` 或数据库 `func.now()` 作为业务入库或展示时间。
- 管理端接口时间统一序列化为 `YYYY-MM-DD HH:mm:ss`；绝对时间比较前必须先统一时区。
- 涉及历史时间数据迁移时，必须说明旧数据的时区语义和补偿策略。

## 8. 测试与验证

- Bug 修复先确认现象、触发条件、预期、实际和根因；适合时补充能复现问题的测试。
- 测试优先覆盖关键路径、边界条件、错误路径和权限边界。
- 未经用户明确指定，不运行测试、静态检查、前端编译或构建命令，包括 `pytest`、`npm test`、`npm run lint`、`tsc` 和 `npm run build`。
- 用户指定验证命令时，必须读取完整结果，确认退出码和失败数量，并在交付说明中如实记录。
- 用户未指定验证命令时，只做代码阅读和非执行式检查；交付时明确说明未运行测试/构建、影响范围和建议验证方式。
- 文档或模块调整可使用 `rg` 检查旧路径和重复实现，使用 `rg --files` 检查目录结构，使用 `git diff --check` 检查差异格式；这些不等同于测试或构建。

## 9. Git 与提交规范

- 未经用户明确要求，不执行 commit、push、merge、rebase、cherry-pick，不创建或删除分支和 worktree。
- 提交必须保持单一目的，不混入无关格式化、重构或生成文件。
- 提交格式：`<type>(scope): <summary>`，`scope` 可省略。
- `summary` 使用简体中文，以动词开头，不超过 50 个字符，不加句号。
- 常用 `type`：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`。
- 示例：`fix(auth): 修复令牌过期判断`。

## 10. 交付要求

交付说明必须包含：

- 修改内容及影响范围
- 关键设计决定或兼容性说明
- 实际执行的验证命令和结果；未执行时说明原因
- 已知风险、限制和建议的后续验证

没有验证证据时，不得声称“测试通过”“可提交”或“可合并”。

## 11. 项目对话与上下文留存

- 与本项目有关的持久上下文统一保存在根目录 `.agents/`，通过 Git 跨设备同步。
- 开始任务时依次读取 `.agents/CONTEXT.md` 和 `.agents/HANDOFF.md`；需要追溯历史时，先查阅 `.agents/INDEX.md`，再按索引读取相关月度记录，不得默认加载全部历史。
- 用户确认的长期约束、关键决策和稳定事实更新到 `.agents/CONTEXT.md`。
- 每次对话任务在交付前，按 `.agents/README.md` 的格式向当月历史文件追加结构化记录，并更新 `.agents/HANDOFF.md`。
- 新建月度历史文件或拆分分卷时，必须同步更新 `.agents/INDEX.md`。
- 只记录项目相关的用户目标、关键讨论、决定、结果、验证和未决事项，不保存思维过程、访问令牌、密码、个人隐私、完整命令输出或其他敏感信息。
- Codex、IDE 或其他客户端自身的会话数据库和运行缓存不属于仓库内容；不得声称仅通过项目文件即可迁移或原生续接这些平台会话。

